// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/message_trailer_array.h"
#include "nodegit_wrapper.cc"
#include "../include/async_libgit2_queue_worker.h"

  #include "../include/message_trailer.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitMessageTrailerArray::~GitMessageTrailerArray() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitMessageTrailerArray::InitializeComponent(v8::Local<v8::Object> target) {
    Nan::HandleScope scope;

    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("MessageTrailerArray").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "trailers", Trailers);
         Nan::SetPrototypeMethod(tpl, "count", Count);
         Nan::SetPrototypeMethod(tpl, "trailerBlock", TrailerBlock);
  
    InitializeTemplate(tpl);

    v8::Local<Function> _constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    constructor_template.Reset(_constructor_template);
    Nan::Set(target, Nan::New("MessageTrailerArray").ToLocalChecked(), _constructor_template);
  }

      NAN_METHOD(GitMessageTrailerArray::Trailers) {
      v8::Local<v8::Value> to;

       git_message_trailer *
          trailers =
          Nan::ObjectWrap::Unwrap<GitMessageTrailerArray>(info.This())->GetValue()->trailers;
 // start convert_to_v8 block
  
  if (trailers != NULL) {
    // GitMessageTrailer trailers
       to = GitMessageTrailer::New(trailers, false , info.This() );
   }
  else {
    to = Nan::Null();
  }

 // end convert_to_v8 block
      info.GetReturnValue().Set(to);
    }
     NAN_METHOD(GitMessageTrailerArray::Count) {
      v8::Local<v8::Value> to;

       size_t
         count =
         Nan::ObjectWrap::Unwrap<GitMessageTrailerArray>(info.This())->GetValue()->count;
 // start convert_to_v8 block
     to = Nan::New<Number>( count);
  // end convert_to_v8 block
      info.GetReturnValue().Set(to);
    }
     NAN_METHOD(GitMessageTrailerArray::TrailerBlock) {
      v8::Local<v8::Value> to;

       char *
         _trailer_block =
         Nan::ObjectWrap::Unwrap<GitMessageTrailerArray>(info.This())->GetValue()->_trailer_block;
 // start convert_to_v8 block
  if (_trailer_block){
       to = Nan::New<String>(_trailer_block).ToLocalChecked();
   }
  else {
    to = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(to);
    }
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitMessageTrailerArrayTraits>;
 