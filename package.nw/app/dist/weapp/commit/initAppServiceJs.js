'use strict';var _exports;function init(){const a=require('fs'),b=require('path');require('glob');const c=require('../utils/tools.js');c.noBrowser.join(','),_exports=(d,e,f)=>{let g=d.projectpath,h=b.join(g,'app.json'),j=JSON.parse(a.readFileSync(h,'utf8')),k=j.pages||[];for(let l=0,m=k.length;l<m;l++){let n=k[l],o=b.join(g,`${n}.js`),p=b.join(g,`${n}.wxml`);if(!a.existsSync(o))return void f(`未找到 app.json 中定义的 ${n} 的脚本文件。`);if(!a.existsSync(p))return void f(`未找到 app.json 中定义的 ${n} 的 wxml 文件。`)}f(null)}}init(),module.exports=_exports;